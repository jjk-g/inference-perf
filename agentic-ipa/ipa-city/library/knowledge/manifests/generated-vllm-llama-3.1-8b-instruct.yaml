apiVersion: v1
kind: Service
metadata:
  labels:
    app: llama-3-1-8b-instruct-vllm-inference-server
  name: llama-3-1-8b-instruct-vllm-service
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: llama-3-1-8b-instruct-vllm-inference-server
    deployment: llama-3-1-8b-instruct-vllm-deployment
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: llama-3-1-8b-instruct-vllm-inference-server
  name: llama-3-1-8b-instruct-vllm-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llama-3-1-8b-instruct-vllm-inference-server
      deployment: llama-3-1-8b-instruct-vllm-deployment
  template:
    metadata:
      labels:
        ai.gke.io/inference-server: vllm
        ai.gke.io/model: LLaMA3_1_8B_Instruct
        app: llama-3-1-8b-instruct-vllm-inference-server
        deployment: llama-3-1-8b-instruct-vllm-deployment
        examples.ai.gke.io/source: blueprints
    spec:
      containers:
      - args:
        - --model=$(MODEL_ID)
        - --tensor-parallel-size=$(TENSOR_PARALLEL_SIZE)
        command:
        - python3
        - -m
        - vllm.entrypoints.openai.api_server
        env:
        - name: TENSOR_PARALLEL_SIZE
          value: "1"
        - name: MODEL_ID
          value: meta-llama/Meta-Llama-3.1-8B-Instruct
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              key: hf_api_token
              name: hf-secret
        image: vllm/vllm-openai:latest
        name: inference-server
        ports:
        - containerPort: 8000
          name: metrics
        readinessProbe:
          failureThreshold: 60
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 10
        resources:
          limits:
            cpu: "8"
            memory: 32Gi
            nvidia.com/gpu: 1
          requests:
            cpu: "4"
            memory: 16Gi
            nvidia.com/gpu: 1
        volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-rtx-pro-6000
      volumes:
      - emptyDir:
          medium: Memory
        name: dshm
